{% extends 'base.html.twig' %}

{% block title %}Hello ProfilController!{% endblock %}

{% block body %}
    {% form_theme editPasswordForm 'bootstrap_5_layout.html.twig' %}
    {% include('home/header.html.twig') %}
    <div class="container col-sm-4 mt-3 mb-3 mt-5 pt-4">
        {{ form_start(editPasswordForm) }}

        {{ form_widget(editPasswordForm)}}

        <div class="btnForm">
            <button class="btnApp" type="submit">Enregistrer</button>
            <a href="{{ path('app_profil_edit', {id: app.user.id}) }}"><button class="btnApp" type="button">Annuler</button></a>
        </div>
        {{ form_end(editPasswordForm) }}
    </div>
{% endblock %}
